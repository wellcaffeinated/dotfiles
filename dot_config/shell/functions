# Shared functions for bash and zsh where possible

mkcd() {
  mkdir -p "$1" && cd "$1"
}

projroot() {
  local root
  root=$(git rev-parse --show-toplevel 2>/dev/null)
  [ -n "$root" ] && cd "$root"
}
